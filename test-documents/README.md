# Test documents

Only synthetic or anonymized CV fixtures may be stored here. The automated Milestone 2 and 3
tests generate ten anonymous PDFs in memory, covering single-column, sidebar and two-column
layouts, then verify their intermediate models and editable DOCX outputs. The `private/`
directory is ignored and must never be committed.
